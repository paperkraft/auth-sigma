import Organization from "@/features/profile/Organization";

export const metadata = {
  title: "Organization",
  description: "Manage your company profile and team workspace settings.",
};

export default function OrganizationPage() {
  return <Organization />;
}
