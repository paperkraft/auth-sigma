import Security from "@/features/profile/Security";

export const metadata = {
  title: "Security",
  description: "Secure your account with passwords and two-factor authentication.",
};

export default function SecurityPage() {
  return <Security />;
}
